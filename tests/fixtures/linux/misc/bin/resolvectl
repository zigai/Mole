#!/bin/bash
# Test fixture: fake resolvectl. Records invocation to MOLE_FIXTURE_MARKER when set.
if [[ -n "${MOLE_FIXTURE_MARKER:-}" ]]; then
    printf 'resolvectl %s\n' "$*" >> "$MOLE_FIXTURE_MARKER"
fi
exit 0
